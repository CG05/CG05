floorHeight = 320.00;
firstHeight = 520.33;
print(f"호텔의 총 높이는 첫 층의 높이( {round(firstHeight, 1)}cm ) + 나머지 14층의 높이( {round(floorHeight, 1)}cm * 14 = {round(floorHeight * 14, 1)}cm )이므로 총 {round((firstHeight + (floorHeight * 14))/100, 1)}m이다.");